info:
  name: Login (Obtain Token)
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/token/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​